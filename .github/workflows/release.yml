name: Build binaries and upload to release

# Manual backfill: automated releases build binaries in release-plz.yml (a
# release-plz tag pushed with GITHUB_TOKEN cannot trigger this workflow). Use
# this to (re)build binaries for an existing release tag, e.g. spaces_client-v0.4.0.
on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to build binaries for (e.g. spaces_client-v0.4.0)'
        required: true
        type: string

jobs:
  release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
          - os: macos-latest
            target: aarch64-apple-darwin

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag }}

      - name: Extract version from tag
        id: get_tag
        run: |
          # tag format: spaces_client-v0.1.0 → VERSION=0.1.0, TAG=<original>
          echo "TAG=${{ inputs.tag }}" >> $GITHUB_ENV
          echo "VERSION=${TAG##*-v}" >> $GITHUB_ENV
        env:
          TAG: ${{ inputs.tag }}

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: ${{ matrix.target }}

      - name: Install cross-compilation tools
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Build release binary
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
        run: |
          cargo build --release --target ${{ matrix.target }} -p spaces_client

      - name: Get OS and architecture
        run: |
          echo "OS=$(echo ${{ matrix.target }} | grep -q 'linux' && echo 'linux' || echo 'darwin')" >> $GITHUB_ENV
          echo "ARCH=$(echo ${{ matrix.target }} | grep -q 'aarch64' && echo 'arm64' || echo 'x86_64')" >> $GITHUB_ENV

      - name: Create release archive
        run: |
          mkdir -p spaces-v${{ env.VERSION }}-${{ env.OS }}-${{ env.ARCH }}
          cp target/${{ matrix.target }}/release/spaced spaces-v${{ env.VERSION }}-${{ env.OS }}-${{ env.ARCH }}/spaced
          cp target/${{ matrix.target }}/release/space-cli spaces-v${{ env.VERSION }}-${{ env.OS }}-${{ env.ARCH }}/space-cli
          tar -czf spaces-v${{ env.VERSION }}-${{ env.OS }}-${{ env.ARCH }}.tar.gz spaces-v${{ env.VERSION }}-${{ env.OS }}-${{ env.ARCH }}

      - name: Upload artifacts to existing release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          files: |
            spaces-v${{ env.VERSION }}-${{ env.OS }}-${{ env.ARCH }}.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}